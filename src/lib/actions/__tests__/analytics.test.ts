import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../analytics'

describe('analytics actions', () => {
  it('should have basic coverage for analytics', async () => {
    expect(true).toBe(true)
  })
})
