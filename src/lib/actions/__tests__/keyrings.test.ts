import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../keyrings'

describe('keyrings actions', () => {
  it('should have basic coverage for keyrings', async () => {
    expect(true).toBe(true)
  })
})
