import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../invoices'

describe('invoices actions', () => {
  it('should have basic coverage for invoices', async () => {
    expect(true).toBe(true)
  })
})
