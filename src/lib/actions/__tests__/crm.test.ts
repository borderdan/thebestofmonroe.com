import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../crm'

describe('crm actions', () => {
  it('should have basic coverage for crm', async () => {
    expect(true).toBe(true)
  })
})
