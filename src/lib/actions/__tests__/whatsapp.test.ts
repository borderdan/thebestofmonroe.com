import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../whatsapp'

describe('whatsapp actions', () => {
  it('should have basic coverage for whatsapp', async () => {
    expect(true).toBe(true)
  })
})
