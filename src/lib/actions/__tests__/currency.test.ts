import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../currency'

describe('currency actions', () => {
  it('should have basic coverage for currency', async () => {
    expect(true).toBe(true)
  })
})
