import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../storage'

describe('storage actions', () => {
  it('should have basic coverage for storage', async () => {
    expect(true).toBe(true)
  })
})
