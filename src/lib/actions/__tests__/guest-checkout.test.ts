import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../guest-checkout'

describe('guest-checkout actions', () => {
  it('should have basic coverage for guest-checkout', async () => {
    expect(true).toBe(true)
  })
})
