import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../admin'

describe('admin actions', () => {
  it('should have basic coverage for admin', async () => {
    expect(true).toBe(true)
  })
})
