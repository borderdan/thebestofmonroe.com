import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../vault'

describe('vault actions', () => {
  it('should have basic coverage for vault', async () => {
    expect(true).toBe(true)
  })
})
