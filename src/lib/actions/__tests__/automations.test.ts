import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../automations'

describe('automations actions', () => {
  it('should have basic coverage for automations', async () => {
    expect(true).toBe(true)
  })
})
