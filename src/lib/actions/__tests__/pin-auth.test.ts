import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../pin-auth'

describe('pin-auth actions', () => {
  it('should have basic coverage for pin-auth', async () => {
    expect(true).toBe(true)
  })
})
