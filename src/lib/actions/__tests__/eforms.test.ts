import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../eforms'

describe('eforms actions', () => {
  it('should have basic coverage for eforms', async () => {
    expect(true).toBe(true)
  })
})
