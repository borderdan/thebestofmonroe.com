import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../crud'

describe('crud actions', () => {
  it('should have basic coverage for crud', async () => {
    expect(true).toBe(true)
  })
})
