import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../inventory'

describe('inventory actions', () => {
  it('should have basic coverage for inventory', async () => {
    expect(true).toBe(true)
  })
})
