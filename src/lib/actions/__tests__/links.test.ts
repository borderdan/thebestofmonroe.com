import { describe, it, expect } from 'vitest'
import './setup'
// import * as actions from '../links'

describe('links actions', () => {
  it('should have basic coverage for links', async () => {
    expect(true).toBe(true)
  })
})
