import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../team'

describe('team actions', () => {
  it('should have basic coverage for team', async () => {
    expect(true).toBe(true)
  })
})
