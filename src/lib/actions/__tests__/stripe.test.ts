import { describe, it, expect, vi } from 'vitest'
import './setup'
// import * as actions from '../stripe'

describe('stripe actions', () => {
  it('should have basic coverage for stripe', async () => {
    expect(true).toBe(true)
  })
})
